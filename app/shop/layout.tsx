export const metadata = {
  title: 'Shop — BuySub',
  description: 'Explore available subscriptions and plans',
};

export default function ShopLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
